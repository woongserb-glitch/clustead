# 데이터(1.4GB)는 이미지에 굽지 않고 런타임 볼륨으로 마운트한다.
data/

# VCS / 캐시 / 가상환경
.git/
.gitignore
__pycache__/
*.pyc
.venv/
venv/

# 시크릿 — 컨테이너엔 compose env_file 로 주입한다.
.env

# 로그·골든 스냅샷·에디터 설정
*.log
flask-server.*.log
tests/golden/
.claude/

# 런타임에 불필요한 산출물(아이콘 미리보기 등). scripts/*.py 는 런타임 import 되므로 제외 금지.
docs/icon-previews/
